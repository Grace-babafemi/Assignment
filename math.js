const add = (first, second, sign) => {
 if (sign === "+") {
    return first + second;
 } else if (sign === "-") {
    return first - second;
 }else if (sign === "*") {
    return first * second;
 }{
    
 }
}